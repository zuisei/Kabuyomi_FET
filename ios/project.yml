name: Kabuyomi
options:
  minimumXcodeGenVersion: 2.45.0
  developmentLanguage: ja
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: 6.0
    PRODUCT_BUNDLE_IDENTIFIER: app.kabuyomi.ios
    CURRENT_PROJECT_VERSION: 6
    MARKETING_VERSION: 1.2
    DEVELOPMENT_TEAM: UGCJZH9KG4
    GENERATE_INFOPLIST_FILE: YES
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
    CODE_SIGN_STYLE: Automatic
    ENABLE_CODE_COVERAGE: NO
    CLANG_COVERAGE_MAPPING: NO
    ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
packages:
  GoogleMobileAds:
    url: https://github.com/googleads/swift-package-manager-google-mobile-ads.git
    version: 13.2.0
schemes:
  Kabuyomi:
    build:
      targets:
        Kabuyomi: all
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: KabuyomiTests
          parallelizable: true
          randomExecutionOrder: true
        - name: KabuyomiUITests
          parallelizable: false
          randomExecutionOrder: false
    analyze:
      config: Debug
    profile:
      config: Release
    archive:
      config: Release
      revealArchiveInOrganizer: false
  KabuyomiUITests:
    build:
      targets:
        Kabuyomi: all
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: KabuyomiUITests
          parallelizable: false
          randomExecutionOrder: false
  KabuyomiStoreKitUITests:
    build:
      targets:
        Kabuyomi: all
    run:
      config: Debug
      storeKitConfiguration: Kabuyomi/StoreKit/KabuyomiTest.storekit
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: KabuyomiUITests
          parallelizable: false
          randomExecutionOrder: false
targets:
  Kabuyomi:
    type: application
    platform: iOS
    sources:
      - path: Kabuyomi
        excludes:
          - StoreKit/KabuyomiTest.storekit
    dependencies:
      - package: GoogleMobileAds
        product: GoogleMobileAds
    info:
      path: Kabuyomi/Info.plist
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleDisplayName: Kabuyomi
        UILaunchScreen:
          UIColorName: ""
          UIImageName: ""
        UIApplicationSupportsIndirectInputEvents: true
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
        ITSAppUsesNonExemptEncryption: false
        KABUYOMI_API_BASE_URL: $(KABUYOMI_API_BASE_URL)
        # 政策タブの向き先。Kabuyomi 本体と同じ形にする。
        # **Release は空のまま。** md-api の本番はまだデプロイされていないので、
        # 埋めない限り政策タブは「設定が無い」と言って止まる。
        # TestFlight 環境を本番ビルドから叩かせない(2026-08-26)。
        MARKET_DOCKET_DEFAULT_ENVIRONMENT: $(MARKET_DOCKET_DEFAULT_ENVIRONMENT)
        MARKET_DOCKET_TESTFLIGHT_API_URL: $(MARKET_DOCKET_TESTFLIGHT_API_URL)
        MARKET_DOCKET_PRODUCTION_API_URL: $(MARKET_DOCKET_PRODUCTION_API_URL)
        LSApplicationCategoryType: public.app-category.finance
        GADApplicationIdentifier: ca-app-pub-1248492954379402~7909080109
        SKAdNetworkItems:
          - SKAdNetworkIdentifier: cstr6suwn9.skadnetwork
    settings:
      base:
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_ENTITLEMENTS: Kabuyomi/Kabuyomi.entitlements
        PRODUCT_NAME: Kabuyomi
        TARGETED_DEVICE_FAMILY: "1"
      configs:
        Debug:
          APP_ATTEST_ENVIRONMENT: development
          KABUYOMI_API_BASE_URL: https://kabuyomi-api-test.dznqjmctk7.workers.dev
          MARKET_DOCKET_DEFAULT_ENVIRONMENT: testflightAPI
          MARKET_DOCKET_TESTFLIGHT_API_URL: https://md-api-testflight.dznqjmctk7.workers.dev
        Release:
          APP_ATTEST_ENVIRONMENT: production
          KABUYOMI_API_BASE_URL: https://kabuyomi-api.dznqjmctk7.workers.dev
          MARKET_DOCKET_DEFAULT_ENVIRONMENT: productionAPI
          MARKET_DOCKET_PRODUCTION_API_URL: https://md-api-prod.dznqjmctk7.workers.dev

    postBuildScripts:
      - name: Generate Ad SDK dSYMs for Archive
        basedOnDependencyAnalysis: false
        script: |
          set -euo pipefail

          # Google distributes these SDKs as stripped binary frameworks through SPM.
          # App Store symbol upload expects matching dSYM bundles in the archive.
          if [ "${PLATFORM_NAME:-}" != "iphoneos" ]; then
            exit 0
          fi

          if [ "${ACTION:-}" != "install" ] && [ -z "${ARCHIVE_PRODUCTS_PATH:-}" ]; then
            exit 0
          fi

          frameworks_dir="${TARGET_BUILD_DIR}/${FRAMEWORKS_FOLDER_PATH}"
          dsym_dir="${DWARF_DSYM_FOLDER_PATH}"
          mkdir -p "${dsym_dir}"

          for framework in GoogleMobileAds UserMessagingPlatform; do
            binary="${frameworks_dir}/${framework}.framework/${framework}"
            output="${dsym_dir}/${framework}.framework.dSYM"

            if [ ! -x "${binary}" ] || [ -d "${output}" ]; then
              continue
            fi

            echo "Generating archive dSYM for ${framework}.framework"
            dsymutil "${binary}" -o "${output}"
          done
  KabuyomiTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: KabuyomiTests
      - path: Kabuyomi/StoreKit/KabuyomiTest.storekit
        buildPhase: resources
    dependencies:
      - target: Kabuyomi
      - sdk: StoreKitTest.framework
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: app.kabuyomi.ios.tests
        PRODUCT_NAME: KabuyomiTests
        TARGETED_DEVICE_FAMILY: "1"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Kabuyomi.app/Kabuyomi"
        BUNDLE_LOADER: "$(TEST_HOST)"
  KabuyomiUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: KabuyomiUITests
      - path: Kabuyomi/StoreKit/KabuyomiTest.storekit
        buildPhase: resources
    dependencies:
      - target: Kabuyomi
      - sdk: StoreKitTest.framework
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: app.kabuyomi.ios.uitests
        PRODUCT_NAME: KabuyomiUITests
        TARGETED_DEVICE_FAMILY: "1"
        TEST_TARGET_NAME: Kabuyomi
