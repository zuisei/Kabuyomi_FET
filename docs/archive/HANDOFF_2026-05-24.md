# Kabuyomi Handoff Note

Last updated: 2026-05-24 JST

This file is a handoff note only.
It is not an implementation plan, edit request, or release approval.

## 1. Current Working Context

- Repo: `/Users/0xt4/t4dano/Kabuyomi`
- Current branch: `main`
- Current visible dirty tracked files:
  - `ios/Kabuyomi/Features/Settings/CreditView.swift`
  - `ios/Kabuyomi/Services/LegalSiteConfig.swift`
  - `ios/KabuyomiTests/AppModelTests.swift`
- Untracked note files/log artifacts also exist in this checkout. Do not treat them as shipping truth automatically.

## 2. What This Product Is

Confirmed from current authoritative docs:

- Kabuyomi v1 is a Japanese iOS app for reading and asking questions about U.S. SEC filings.
- Current supported filing scope is centered on `10-K` and `10-Q`.
- Official iOS route is:
  - `AppRootView -> ConversationEntryView -> CompanyView`
- Official backend route is:
  - `iOS -> Workers -> sec-fetcher -> SEC`
  - `Workers -> Gemini / local fallback`
  - `Workers -> KV / D1 / R2 / DO`

Primary source:
- `docs/release/CURRENT_SHIPPING_TRUTH.md`

## 3. Current Shipping Truth To Trust First

Prefer these files over older reports or audit-style notes:

- `AGENTS.md`
- `CURRENT_SLICE.md`
- `docs/release/CURRENT_SHIPPING_TRUTH.md`
- `docs/archive/v1/TESTFLIGHT_READINESS_CHECKLIST.md`
- `ios/project.yml`
- `ios/Kabuyomi/App/AppRootView.swift`
- `ios/Kabuyomi/App/AppModel.swift`
- `workers/src/index.ts`
- `workers/src/routes/*`
- `sec-fetcher/server.mjs`
- `README.md`

If another note conflicts with these, prefer current code plus the files above.

## 4. Billing / Monetization Snapshot

Confirmed from `docs/release/CURRENT_SHIPPING_TRUTH.md`:

- Billing is enabled in the current v1 path.
- iOS uses StoreKit for consumables and subscriptions.
- `kabuyomi.credits.50` is the primary paid credit pack.
- `kabuyomi.credits.100` remains as a compatibility product when present.
- Monthly subscription plans exist for Lite / Pro / Max.
- Workers verify Apple transactions before granting paid credits.
- Normal chat cost is `2` credits.
- Rewarded-ad backend routes exist, but rewarded-credit UI is hidden in the current RC / App Review path until real Google SSV grant evidence is recorded.

## 5. Runtime Status

Confirmed by existing repo docs, not re-verified in this handoff session:

- A live Worker URL existed at the time of the current shipping truth note.
- Core live path was documented as present for `search`, `usage`, `company`, `chat`, and billing sync / entitlement behavior.

Still unverified in this handoff session:

- deployed Worker parity with local working tree
- live `remote_config`
- staging parity
- starter ticker quality sweep
- final TestFlight parity
- current production parity after any later deploy

If runtime has not been directly checked, keep it as `未確認`.

## 6. Important Reading of Existing Notes

- `docs/archive/SYSTEM_AUDIT_2026-05-22.md` is useful as a broad architecture-oriented reference.
- Do not treat `docs/archive/SYSTEM_AUDIT_2026-05-22.md` as the single source of truth.
- It is stronger on system-shape summary than on evidence discipline and current-state precision.
- Older release reports and audit reports may contain historical truths that are no longer the current RC truth.

## 7. Practical Handoff Guidance

- Start from `docs/release/CURRENT_SHIPPING_TRUTH.md`.
- Then inspect the current dirty tracked files listed at the top of this note.
- Separate:
  - current code truth
  - confirmed runtime truth
  - unverified assumptions
- Do not claim deploy parity or runtime parity unless re-checked directly.
- Do not infer product scope from stale historical docs when current code or current shipping truth disagrees.
